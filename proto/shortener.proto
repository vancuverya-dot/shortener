edition = "2023";

package shortener;

option go_package = "github.com/vancuverya-dot/shortener/internal/grpcapi/proto";

import "google/protobuf/go_features.proto";

option features.(pb.go).api_level = API_OPAQUE;

service ShortenerService {
  rpc ShortenURL (URLShortenRequest) returns (URLShortenResponse);
  rpc ExpandURL (URLExpandRequest) returns (URLExpandResponse);
  rpc ListUserURLs (UserURLsRequest) returns (UserURLsResponse);
}

message URLShortenRequest {
  string url = 1;
}

message URLShortenResponse {
  string result = 1;
}

message URLExpandRequest {
  string id = 1;
}

message URLExpandResponse {
  string result = 1;
}

// UserURLsRequest намеренно не содержит полей: собственный тип вместо
// google.protobuf.Empty позволяет в будущем добавить параметры
// (пагинацию, фильтры) без нарушения обратной совместимости.
message UserURLsRequest {
}

message UserURLsResponse {
  repeated URLData url = 1;
}

message URLData {
  string short_url = 1;
  string original_url = 2;
}